# Deno
.deno/

# Node tooling (tools/ document generators)
node_modules/

# Env / secrets — never commit
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.swp

# Deno lockfile — deliberately untracked: sandbox/CI resolve different registries
deno.lock
